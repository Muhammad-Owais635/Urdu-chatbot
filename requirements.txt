Flask==3.0.3
scikit-learn==1.5.1
joblib==1.4.2
python-dotenv==1.0.1
flask-cors==4.0.1
flask-limiter==3.8.0
gunicorn==22.0.0

# Optional — only needed if LLM_FALLBACK_ENABLED=true
openai==1.40.0

# Optional — only needed for bert_classifier.py upgrade path
# transformers==4.44.0
# torch==2.4.0

# Dev/test dependencies
pytest==8.3.2
